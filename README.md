# ResumeAI

An open-source AI-powered resume builder that runs locally on your machine using Ollama LLM models. It consists of a FastAPI backend and a React + Vite frontend.

## About

ResumeAI is a lightweight project that combines a Python backend with a modern React frontend. It uses local AI models (Ollama) for processing, so everything runs on your machine with no cloud dependency.

## Tech Stack

- **Backend**: FastAPI with Python
- **Frontend**: React + Vite with SCSS
- **AI/LLM**: Ollama with llama3 (or any Ollama model)
- **Database**: SQLAlchemy + SQLite

## What's Inside

- A Resume Builder which uses Local ollama LLM models like llama3 to generate resume content based on user input.
- A simple React frontend to interact with the backend API.

## Getting Started

For setup instructions, see:
- [backend/README.md](backend/README.md) - How to run the backend
- [frontend/README.md](frontend/README.md) - How to run the web app

Then follow each directory's README.